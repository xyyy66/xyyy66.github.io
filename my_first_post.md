# 学习如何使用 Markdown 写作

这是我的第一篇 Markdown 博文。在这个文件中，我不需要再忍受繁琐的 HTML 标签了！

## 为什么使用 Markdown？

Markdown 是一种轻量级的标记语言，让我可以专注于**内容本身**，而不是去记忆复杂的网页排版。

### 测试代码高亮

这里是一个简单的 C 语言程序，测试高亮是否正常：

```c
#include <stdio.h>

int main() {
    printf("Markdown 真是太棒了！\n");
    return 0;
}
```

这里是一个 Python 脚本的例子：

```python
def greet(name):
    print(f"Hello, {name}! This is from a Markdown file.")

greet("Xu Yan")
```

---

*希望这篇教程对你有帮助。这只是一段演示文本。*